<h2 style=" color: #FFFFFF">Reset Password</h2>
<h3>@ViewBag.Message</h3>

@using (Html.BeginForm("ResetPassword", "AccountLogin", new { token = ViewBag.token }, FormMethod.Post))
{
    <input type="hidden" name="token" value="@ViewBag.token">

    <label style="color:#FFFFFF" for="NewPassword">New Password:</label>
    <input type="password" id="NewPassword" name="NewPassword" required />

    <label style="color:#FFFFFF" for="NewPasswordConfirmation">Confirm New Password:</label>
    <input type="password" id="NewPasswordConfirmation" name="NewPasswordConfirmation" required />

    <button class="btn btn-blue-md" type="submit">Save Changes</button>
}

<style>
    .btn-blue-md {
        display: block; /* Makes each button a block-level element */
        width: 100%; /* Ensures each button stretches to the full width of its container */
        padding: 14px 20px;
        font-size: 14px;
        line-height: 1.333333;
        color: white;
        background-color: #2f4d5a;
        border: 4px solid #FFFFFF;
        border-radius: 75px;
        margin-bottom: 10px; /* Adds space between the buttons */
        text-align: center; /* Center the text in the button */
    }
</style>